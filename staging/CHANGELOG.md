# EWC Staging Changelog

| Page edited | Old section | New section | Reason for change |
| --- | --- | --- | --- |
| Homepage (`/`) | Mixed brand language, vague hero, buried proof, unclear Free vs Elite presentation, legacy-style FAQ and trust copy | Premium dark landing page with clear hero, proof band, member benefits, differentiators, analyst credibility, case-study preview, Free vs Elite pricing, concise FAQ, and final trust CTA | Clarify the offer in seconds, surface proof immediately, standardize the brand, and make the primary conversion paths obvious for cold traffic |
| Case Studies (`/previous-projections/`) | Long archive of analyst notes and repeated before/after sections with weak hierarchy | Curated case-study page with strong headline, proof framing, four structured case-study cards, before/after chart pairs, and upgrade CTA | Repackage trade setups into cleaner proof assets that feel premium, readable, and conversion-supportive instead of blog-like |
| Membership (`/service/`) | Outdated service page with inconsistent pricing story, weak copy, and old CTA path | Focused membership page with proof stats, clear Free vs Elite comparison, offer breakdown, objection-handling FAQ, and trust/disclaimer close | Remove pricing confusion, align the offer story with the homepage, and stop an old public-facing page from undercutting trust |
| Homepage (`/`) | Premium dark landing page with solid structure but flatter spacing, softer hero specificity, text-heavy case-study previews, and weak pricing contrast | Refined premium homepage with sharper value proposition, larger spacing, image-led case-study proof cards, stronger Elite contrast, cleaner trust language, and lower text density | Increase perceived quality, make the value obvious faster, and improve conversion clarity without changing the page architecture |
| Case Studies (`/previous-projections/`) | Cleaner case-study format with limited visual outcome emphasis and heavier note-style copy | More visual proof page with stronger hero, outcome-led result panels, cleaner before/after presentation, and improved premium spacing | Make proof feel more polished, easier to scan, and more persuasive for cold traffic |
| Membership (`/service/`) | Clear comparison page with modest visual separation between Free and Elite and some transitional copy | More premium offer page with sharper hero, stronger Free vs Elite contrast, cleaner copy, better spacing, and upgraded trust presentation | Make the pricing story immediately obvious and align the offer page with the refined homepage aesthetic |
| Homepage (`/`) | Over-refined visual treatment with heavier spacing and stronger design changes than desired | Restored the earlier staging homepage and kept only a small FAQ wording cleanup | Return to the stronger original version while removing copy that sounded unfinished |
| Case Studies (`/previous-projections/`) | Heavier refinement pass with more dramatic proof-card treatment | Restored the earlier staging case-studies page | Return to the original presentation the user preferred |
| Membership (`/service/`) | Heavier refinement pass with stronger restyle and pricing treatment | Restored the earlier staging membership page and kept only small wording cleanups in the offer/FAQ copy | Return to the earlier design while preserving the minor copy improvements |
| Homepage (`/`) | Earlier homepage with standard top nav, simpler two-column hero, conventional pricing grid, and flatter section hierarchy | Island-layout homepage with floating rounded header, oversized hero, premium dark card system, image-led proof, and stacked pricing cards with highlighted Elite treatment | Shift the homepage toward a more premium, modern conversion-focused layout while preserving Elliott Wave Club content and brand direction |
| Homepage (`/`) | Island-layout homepage with redesigned hero, spacing, pricing stack, card system, and mobile layout | Restored the pre-island homepage design and content while preserving the new floating island header/navbar | Keep the header direction the user liked without carrying over the broader homepage redesign |
| Homepage (`/`) | Generic framework section explaining why EWC feels different | Elliott Wave 2.0 section repositioned around a more mechanical market-reading framework, with updated body copy and four new support cards | Clarify the Elliott Wave 2.0 concept in more practical, rules-based language without redesigning the homepage around it |
| Homepage (`/`) | CTA buttons slightly washed out, pricing card labels cramped, Trigger Trades image not fully filling its frame, and homepage info cards feeling uneven | Subtle homepage polish with softer higher-contrast CTA buttons, more space under Free/Elite label pills, corrected Trigger Trades image fill, and more consistent text alignment across homepage info cards | Improve readability and polish while leaving the homepage structure intact and avoiding changes to the case studies section |
| Homepage (`/`) | Member benefit cards under “What members get” felt uneven, and homepage CTA buttons still looked too washed out | Refined the member-card grid with more uniform card sizing, padding, title/body alignment, and stronger CTA button contrast across shared homepage CTAs | Keep the current homepage design intact while making the card row feel balanced and the CTA labels easier to read |
| Homepage (`/`) | Elite price suffix sat too close to `$99` and felt slightly cramped | Added a dedicated price-period style so `/month` has cleaner spacing and baseline alignment next to the main price | Improve polish in the Elite pricing card without changing the page layout or typography system |
| Homepage (`/`) | Homepage headings and card titles mixed sentence case and title case | Standardized homepage section headings, card titles, and related title text to one consistent Title Case system while preserving uppercase eyebrow styling | Make the homepage feel cleaner and more intentional without changing layout, wording, or page structure |
| Homepage (`/`) | Mobile island header links wrapped awkwardly, spacing felt crowded, and the CTA placement was messy on small screens | Reworked the mobile island header into a compact single-row bar with a menu toggle and a clean dropdown nav panel containing the links and CTA, while leaving the desktop island layout intact | Make the homepage header feel polished and intentional on mobile without redesigning the rest of the page |
| Homepage (`/`) | Public rating proof still showed `4.75/5` in the hero proof band and stats row | Updated the homepage rating proof to `4.8/5` in both visible locations | Keep the homepage proof figures aligned with the requested rounded rating display |
| Case Studies (`/previous-projections/`) | Legacy theme header/footer still showed, hero proof stat listed only 4 examples, and 9 requested case studies were missing from the curated archive | Replaced the legacy page chrome with the homepage island nav, updated the proof count to 13, hid the theme footer/header to match the homepage treatment, and added case studies 5-13 in the existing case-card format with before/after images | Bring the case-studies page into alignment with the homepage navigation while expanding the curated proof archive without changing the page design system |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | Some CTA buttons looked washed out or inconsistent across the two public pages | Standardized CTA buttons on both pages to the homepage hero primary/secondary styles and aligned nav CTA fills to that same primary style | Improve button consistency across the public landing pages without changing page layouts or content structure |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | Homepage nav lacked a Case Studies link, homepage pricing still showed only two tiers, testimonials/custom analysis/footer were missing, and the case-studies page still needed the shared footer/stat update | Added `Case Studies` to the shared island nav, fixed the homepage `View All Case Studies` link, expanded homepage pricing to Free + Elite Monthly + Elite Yearly + Elite Lifetime, added Custom Chart Analysis, added member testimonials, added the Trigger Trades X link, added the shared minimal footer to both pages, updated the case-studies market stat to `SPX, NQ, ES, SMH`, and added extra top spacing under the case-studies island nav | Bring the homepage and case-studies page into the requested shared site structure while preserving the existing design system and case-study layouts |
| Homepage (`/`) | Elite pricing cards included extra explanatory list lines and a monthly checkout note that made the pricing copy heavier than needed | Removed the `Positioning` and `Checkout` list rows from the Elite Monthly, Elite Yearly, and Elite Lifetime cards, and removed the monthly checkout note | Tighten the homepage pricing copy without changing the layout, prices, or buttons |
| Homepage (`/`) | The five member-benefit cards under `What Members Get Inside Elliott Wave Club` did not align as cleanly as they should across desktop and mobile | Tightened the `#inside` card-grid CSS so all five cards share consistent internal padding, equal-height behavior, uniform row gaps, and cleaner stacked spacing on mobile | Improve spacing and alignment without changing content, colors, typography, or the overall design |
| Homepage (`/`) | The three `Why Members Stay` quote cards were visually clean but lacked a stronger scan anchor | Added a subtle teal vertical accent line to the left edge of each `Why Members Stay` quote card using a pseudo-element | Improve scanability and visual rhythm without changing the text, spacing, or card layout |
| Homepage (`/`), Case Studies (`/previous-projections/`), and Membership (`/service/`) | The shared nav CTA and label accents still felt too muted, and key proof accents did not use a consistent secondary brand color | Changed the shared `Join Elite` nav CTA to a white-on-dark treatment, added a restrained teal accent to the homepage `Start Free` buttons, homepage stat numbers, the `Best Value` badge, the `Custom Add-On` pill, homepage section eyebrow pills, case-studies eyebrow pills, and membership hero/trust eyebrow pills | Introduce a more intentional sitewide accent rhythm while keeping the dark navy system dominant and leaving copy and layout unchanged |
| Homepage (`/`) | The hero brand pill repeated the site name directly below the nav and crowded the top of the hero copy | Removed the redundant `Elliott Wave Club` eyebrow pill above the main hero headline | Let the homepage hero headline sit higher and breathe more without changing the headline, subtext, or CTAs |
| Homepage (`/`) | The four pricing cards under `Choose Your Path` had inconsistent internal spacing, misaligned price/note rows, and uneven CTA/button baselines | Normalized the pricing-card vertical rhythm so the badge row, headline, price line, note slot, description, feature list, and CTA button align consistently across all four cards, including tightening the Lifetime `one-time` lockup | Improve pricing-card spacing and alignment without changing visible copy, colors, buttons, or layout structure |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | The shared island header needed cleaner vertical alignment, more breathing room between nav links, a stronger nav CTA hover state, and a subtler sense of live activity in the brand mark | Tuned the shared header CSS so the brand and nav links sit on the same center line, increased nav-link spacing, added a teal hover state to the white `Join Elite` nav button, and added a subtle pulse animation to the green brand dot | Improve header polish and clarity across the pages that use the shared island navigation without changing nav content, links, or structure |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | The mobile dropdown menu still used chunky pill-style link blocks, and the homepage pricing cards felt over-padded on phones | Simplified the mobile nav links into a tighter plain-text stack with a leaner full-width CTA button, and reduced homepage mobile pricing-card padding and vertical spacing so the cards feel more compact and scannable | Improve phone-size usability without changing desktop layout, content, or section structure |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | The mobile dropdown still opened as a full-width dark sheet and felt heavier than the island header itself | Rebuilt the mobile nav as a compact floating light dropdown card anchored to the top-right of the header, moved `Join Elite` into a small top-row CTA, added an internal close button plus dimmed page overlay, and kept the hamburger icon stable instead of morphing into an X | Make the mobile menu feel lighter, more intentional, and more premium without changing desktop navigation, links, or page content |
| Homepage (`/`) | The `What Members Get` and `A More Mechanical Way to Read the Market` sections were still text-heavy and lacked direct visual proof of setups resolving | Added two responsive before/after image pairs under the `What Members Get` cards and the `Elliott Wave 2.0` proof cards, using the requested staging chart URLs plus short captions, while keeping all existing copy, section order, and styling intact | Break up dense text with visual examples without changing the homepage’s content, layout structure, or design system |
| Homepage (`/`) | The AMZN before/after images under `What Members Get` were reversed, and the `Elliott Wave 2.0` pill labels still looked like interactive buttons | Swapped the two AMZN image positions so the pair reads in the correct order and restyled the four `Elliott Wave 2.0` pills as subtle teal category labels with no button chrome | Fix two homepage-only clarity issues without changing any other copy, layout, or design elements |
| Homepage (`/`) | The pricing cards still had uneven badge alignment, inconsistent price/unit baselines, redundant paid-card list rows, and CTA buttons that did not anchor to the same bottom position | Reworked the pricing-card grid rows so the badge row, headline, price line, note, description, optional free-card list, and CTA button all align consistently; tightened the paid price lockups into single-line bottom-aligned rows; removed the redundant `Included:` list from the three paid cards; and centered the `Elite Yearly` badge row on one line | Make the four homepage pricing cards read as one aligned system without changing links, colors, or button styling |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | The shared header still used the old nav names/order, the desktop CTA sat on the wrong side of the link cluster, and the mobile dropdown did not match the requested site navigation | Reordered the shared island header so the brand stays on the left, the center nav uses `Case Studies`, `Membership`, `Process`, `Pricing`, and `FAQ`, the `Join Elite` CTA sits on the far right on desktop, and the mobile dropdown mirrors the same link names and order | Apply the requested sitewide header navigation labels and layout without changing any non-header content or sections |
| Homepage (`/`) | The `Why Members Stay` section was no longer needed, and the mobile dropdown `Join Elite` CTA still sat off-axis relative to the nav links | Removed the entire `Why Members Stay` section and centered the homepage mobile `Join Elite` CTA within the hamburger panel so it lines up cleanly with the menu items | Clean up the homepage flow and make the mobile island menu feel more balanced without changing any other sections or content |
| Homepage (`/`) | The homepage background still used a brighter blue-heavy top fade that felt less cohesive with the darker chart aesthetic | Updated the homepage root background gradient to fade from `#12203D` into `#121416` with a subtler supporting blue glow | Make the homepage backdrop feel darker, more premium, and more consistent with the chart tone without changing layout or contrast |
| Homepage (`/`) | The old `What Members Get Inside Elliott Wave Club` section still used generic feature cards and did not clearly separate the free path from the Elite membership | Replaced that section with `What You Unlock Inside Elliott Wave Club`, updated the intro copy to explain the two-tier ecosystem, and swapped the five generic cards for two more specific `Free Community` and `EWC Elite` cards with detailed feature lists | Make the free vs Elite value split immediately clear without redesigning the rest of the homepage |
| Homepage (`/`) | The hero and the `Elliott Wave 2.0` section were repeating the same positioning instead of working as a clean one-two message flow | Changed the hero to `Introducing Elliott Wave 2.0` with a tighter subheadline, rewrote the `Elliott Wave 2.0` section to `What Makes Elliott Wave 2.0 Different`, reduced the body copy to two sharper paragraphs, and tightened the four supporting card descriptions | Reduce repetition and make the hero introduce the concept while the section below explains it without redesigning the page |
| Homepage (`/`) | The hero still led with the product name instead of the user benefit, Free CTAs were visually too strong in lower-funnel offer sections, and `Custom Chart Analysis` was still mixed into the membership pricing area | Updated the hero to a benefit-led `Trade With a Mechanical Edge Using Elliott Wave 2.0` headline with `Introducing Elliott Wave 2.0` as the eyebrow, shifted CTA hierarchy so Elite is primary in hero/pricing/final offer contexts while Free remains visible as the quieter option, and moved `Custom Chart Analysis` into its own `A La Carte Services` section below the membership offers | Improve conversion flow and offer clarity without redesigning the homepage |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | Chart images could not be inspected closely, which weakened proof readability on mobile and desktop | Added click-to-enlarge lightboxes to the homepage proof-chart image pairs and the full case-studies page before/after charts, preserving the existing layout while allowing large modal inspection with keyboard and click-to-close behavior | Make proof assets easier to inspect without changing card layouts or copy |
| Homepage (`/`) | The pricing section repeated Elite features across multiple cards and made the offer harder to scan than necessary | Rebuilt the pricing area into a two-card split with a secondary `Free` card on the left and a wider `EWC Elite` card on the right, stated the Elite feature set once, stacked the Monthly / Yearly / Lifetime purchase rows inside the Elite card, favored the Yearly row visually, and kept `Custom Chart Analysis` separate below in `A La Carte Services` | Make the pricing offer feel cleaner, more premium, and easier to compare while keeping the page biased toward Elite conversions |
| Homepage (`/`) | The large `EWC Elite` card still needed cleaner internal alignment between the shared feature list and the stacked pricing tiers | Refactored the Elite card so the wrapper is a flex column with unified padding, added a full-width header block, rebuilt the inner body as a two-column features/pricing grid, made the pricing tiers a flex-column stack, increased feature-list line-height and spacing, aligned the top of the feature list to the top of the Monthly tier, and added a dedicated `<768px` inner-grid stack | Tighten the Elite card’s scan pattern and alignment without changing the offer content or the rest of the homepage |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | The public footer still exposed the old Gmail contact address on staging | Replaced the visible footer email text and `mailto:` links with `support@elliottwaveclub.com` | Standardize the public support address without changing any other content or layout |
| Shared Elementor Footer (`elementor_library` `#192`) and Membership (`/service/`) | The shared site footer still exposed the old Gmail address on the service page after the page-level footer updates | Replaced the footer library contact email with `support@elliottwaveclub.com` and republished the membership page so the shared footer render refreshed publicly | Remove the last visible Gmail support reference from staging without changing any other page content |
| Homepage (`/`) | The large `EWC Elite` pricing card still felt slightly disjointed internally, with uneven spacing between the header, feature list, and pricing tiers | Tightened the Elite card header rhythm, rebalanced the inner two-column grid, slightly condensed the feature-list spacing, introduced a dedicated tier-body wrapper for cleaner price/note alignment, tightened the tier stack gaps, and added small mobile guardrails so the card stays balanced without changing copy or layout | Make the Elite pricing card feel like one composed premium offer instead of two loosely grouped blocks |
| Homepage (`/`) | The inner Monthly / Yearly / Lifetime tier rows still had awkward text-button pressure, inconsistent tag alignment, and uneven price-suffix formatting | Refactored each tier row to a strict flex-based text/button split, wrapped the text side in a dedicated flex column, aligned the title and tag pills on one row, moved the price into a separate amount/suffix baseline container, and added a dedicated action wrapper so the CTA stays centered without squeezing the text | Eliminate overlap risk and make the three inner pricing tiers read as one consistent premium system |
| Homepage (`/`) | The `Most Popular` badge in the Monthly row still sat too close to the plan name instead of reading like the right-aligned Yearly badge | Updated the tier top-row alignment so the plan title stays left and the badge is pushed to the far right with the same spacing behavior used for the Yearly badge treatment | Make the Monthly badge placement feel intentional without changing the rest of the pricing layout |
| Homepage (`/`) | The Elite pricing stack still relied on row-specific spacing and did not fully share one internal layout template across Monthly, Yearly, and Lifetime | Rebuilt the three right-side Elite pricing rows on one strict 3-zone grid with a shared plan-plus-badge lane, a consistent price-plus-note lane, fixed placeholder slots for the empty Lifetime badge/note, matched CTA widths, and uniform row heights | Replace repeated spacing tweaks with one consistent structural row system so all three Elite plans align cleanly |
| Homepage (`/`) | The `Choose Your Path` block needed a clean reset to an exact two-card offer structure with simpler Free copy and a clearer Elite feature-plus-tier stack | Replaced the pricing section with a two-card `Free` / `EWC Elite` layout, trimmed the Free card to the exact requested description and three bullets, kept the Elite card to one shared four-bullet feature list, and rebuilt the Monthly / Yearly / Lifetime rows as aligned left-badge / center-price / right-CTA sub-cards using the requested plan labels, price strings, and links | Restore the pricing section to the exact requested structure without changing any other homepage sections |
| Homepage (`/`) | The last pricing-stack rebuild introduced a worse row system with misplaced badges, price/button collisions, and weaker alignment than the earlier two-card version | Reverted the pricing section to the cleaner earlier two-card `Free` / `EWC Elite` baseline, restored the fuller Free card copy and Elite feature stack, and then refined the three Elite rows to a simple top-line plan/badge, middle-line price, and bottom-line note/CTA template with consistent CTA widths and aligned suffixes | Recover the previous cleaner pricing baseline first, then make a controlled refinement without redesigning the rest of the homepage |
| Homepage (`/`) | The right-side Elite pricing stack still read as three separate floating mini-cards instead of one unified pricing module | Converted the three Elite tiers into one shared outer pricing panel with subtle internal dividers, removed the individual floating-card chrome from each row, and kept the same top-line badge placement, aligned price rows, and bottom-line note/CTA structure inside the unified shell | Make the Elite pricing side feel like one premium module without changing the overall two-card pricing layout |
| Homepage (`/`) | Inside the unified Elite pricing panel, the badges still felt detached from the plan labels, the CTA sat too low relative to the price, and the stronger row emphasis was still on Yearly instead of Monthly | Moved the `Most Popular` and `Best Value` badges directly beside their plan names, shifted each `Join Elite` CTA onto the same main line as the price, left the savings note as its own lower line, and moved the stronger highlight plus accent CTA treatment from Yearly to Monthly | Tighten the internal hierarchy of the unified Elite pricing module without changing the rest of the pricing section or homepage |
| Homepage (`/`) | The `Join Elite` buttons in the unified Elite pricing panel were overflowing past the right edge of the pricing box at several desktop widths | Rebalanced the Elite features/pricing column split, reduced the tier-row horizontal padding and CTA width, tightened the price sizing slightly, and constrained the pricing rows with border-box sizing so each CTA stays on the same line as the price while remaining fully inside the pricing panel | Keep the CTA aligned horizontally with the price while containing the buttons cleanly inside the pricing module |
| Homepage (`/`) | The header still used the placeholder blue dot instead of the Elliott Wave Club mark, and the `Introducing Elliott Wave 2.0` eyebrow was sitting in the hero instead of the explanatory section below | Replaced the navbar dot with a compact inline version of the Elliott Wave Club logo, removed the hero eyebrow and tightened the hero headline spacing, and moved the `INTRODUCING ELLIOTT WAVE 2.0` eyebrow to the `What Makes Elliott Wave 2.0 Different` section | Use the actual brand mark in the header and move the introductory label to the section where it better supports the copy flow |
| Homepage (`/`) | The case-study preview titles needed stronger marketing language, the `A La Carte Services` block no longer fit the homepage flow, and the Elliott Wave 2.0 eyebrow pill was stretching too wide | Rewrote the second and third homepage case-study titles to be more outcome-driven, removed the `A La Carte Services` / `Custom Chart Analysis` section, tightened section-header eyebrow sizing so the Elliott Wave 2.0 pill hugs its text, and added a new light-card `Seamless Integrations` section between pricing and testimonials with Discord, YouTube, and Whop visual cards plus the requested CTA copy | Strengthen the homepage proof messaging, remove the extra service section, and add a cleaner platform-access section without redesigning the rest of the page |
| Homepage (`/`) | The AMZN proof visual under the member-unlock section was still using the smaller split before/after images instead of the larger combined chart-plus-alert asset | Replaced the AMZN before/after proof pair with the single uploaded `Ads-Q1-2026-scaled.png` image, switched that proof block to a single-column media layout, and kept the existing caption and surrounding section structure intact | Use the larger combined AMZN chart/callout image without redesigning the homepage section |
| Homepage (`/`) | The integrations CTA felt too dark, the final `Start Here` eyebrow was stretching across the card, the analyst eyebrow needed better alignment with its headline, and the second and third proof cards still lacked point-specific outcome framing | Brightened the `Get Started Now` integrations CTA, constrained the final `Start Here` eyebrow to a true pill width, aligned the `About the Analyst` eyebrow as a tighter top label with more breathing room below it, and updated the two right-side case-study preview titles/results to include rounded point outcomes (`400 NQ points` and `50 ES points`) | Clean up the remaining homepage polish issues without changing the page structure or touching any other page |
| Homepage (`/`) | The AMZN proof row under the member-unlock section had been collapsed to a single image, which removed the resolved-outcome visual | Restored the AMZN proof block to a connected before/after pair by keeping the larger Discord-callout chart as the `BEFORE` image, bringing back the decline/result chart as the `AFTER` image, and giving that specific row a slightly wider left column so the live-callout image stays primary without breaking the shared proof styling | Preserve the stronger AMZN live-callout visual while restoring the outcome confirmation inside the same case-study block |
| Homepage (`/`) | The restored AMZN before/after pair used an uneven column split that made the `AFTER` chart look smaller and broke the visual comparison | Reset the AMZN proof row to an equal two-column grid and made both proof cards use the same flex-height container so the `BEFORE` and `AFTER` charts render as a matched pair with balanced sizing | Keep the larger callout chart while restoring a clean, premium before/after comparison without redesigning the section |
| Homepage (`/`) | The bullet lists inside the `What You Unlock Inside Elliott Wave Club` Free and Elite cards were clear but visually flat and harder to scan than the rest of the page | Refined the list typography by splitting key bullets into stronger lead lines plus quieter supporting phrases, slightly increasing list spacing, and styling the lead text with a brighter, more premium weight while keeping the card layout and meaning unchanged | Make the Free and Elite value cards feel higher-value and more scannable at a glance without redesigning the section |
| Homepage (`/`) | The revised split-line bullet treatment in the `What You Unlock Inside Elliott Wave Club` cards felt less clean than the simpler original list style | Restored the original single-line bullet copy in both the Free and Elite cards, then replaced the plain dot markers with subtle circular check-mark icons inspired by the reference styling while keeping the card layout unchanged | Bring back the cleaner original card feel while making the lists look more intentional and premium at a glance |
| Homepage (`/`) | The upper homepage background still started from the older darker blue tone instead of the requested richer navy | Updated the homepage wrapper gradient so the top half now begins with `#1e3347`, adjusted the supporting top radial glows to the same hue family, and kept the lower fade anchored to the existing dark finish | Shift the upper homepage tone to the requested richer navy without flattening the page or changing the existing dark fade |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | The shared navbar logo was rendering from a placeholder/distorted mark instead of the provided transparent PNG, which made the brand mark feel cropped and mis-sized | Replaced the shared navbar mark with the provided transparent PNG logo, moved it into a fixed logo container with `object-fit: contain`, increased the gap between the mark and brand text slightly, and aligned the container to the same vertical center line as the `Elliott Wave Club` wordmark | Make the shared staging navbar logo render cleanly at its natural aspect ratio without redesigning the header |
| Homepage (`/`) and Case Studies (`/previous-projections/`) | The corrected navbar logo was still reading too small inside the shared island header | Doubled the desktop brand-mark container from `32px` to `64px`, increased the logo-to-wordmark spacing slightly, and added a smaller `52px` mobile fallback so the header stays clean on narrow screens | Make the shared staging navbar logo substantially more prominent without changing the rest of the header design |
| Homepage (`/`) | The membership split explained Free vs Elite clearly, but it still didn’t show the actual structure of the Discord ecosystem in a polished, curated way | Added a new `Inside the EWC Ecosystem` section directly below `What You Unlock Inside Elliott Wave Club`, built as two premium dark mockup panels: one for the `Public Community Preview` and one for the `Elite Trading Floor Preview`, each using curated channel groups and clean module cards inspired by the shared Discord screenshots instead of raw screenshot dumps | Show the depth and professionalism of the Elliott Wave Club community structure without breaking the homepage design system or using cluttered raw Discord captures |
| Homepage (`/`) | The newly added Discord/community preview section added visual weight the user did not want, and several homepage `archive` references still implied a formal archive offer | Removed the entire `Inside the EWC Ecosystem` section, rebalanced the homepage flow by letting `What You Unlock` lead directly into `What Makes Elliott Wave 2.0 Different`, replaced the `Archive` proof/stat card with `Models` using the supplied copy, and updated the remaining homepage copy to remove archive-offer implications in the hero proof pills, case-study intro, credibility blurb, Elite feature list, and FAQ answer | Tighten the homepage back down while making the offer language more accurate to the real product |
| Homepage (`/`) | On small screens, the `What You Unlock Inside Elliott Wave Club` Free and Elite cards were becoming a long wall of text because the full desktop bullet lists carried straight into mobile | Added condensed mobile-only lists for both cards using the requested 5-line versions, kept the full detailed lists for desktop, tightened bullet spacing and checkmark size on mobile, and reduced the card padding slightly below `767px` | Make the membership split easier to scan on phones without weakening the desktop section or redesigning the cards |
